# Sign‑In Pages & Login

Un petit projet Flask pour rigoler : un système simple d’inscription (`signup`) et de connexion (`login`) avec affichage du mot de passe (oui, pour le troll 😄).

---

## 🔍 Description

Ce projet permet de :

- Créer un compte avec un nom d'utilisateur (`prenom`) et un mot de passe.  
- Se connecter avec ces identifiants.  
- Afficher le mot de passe de l’utilisateur dans la page de connexion (juste pour le fun).  
- Afficher le mot de passe juste après l’inscription.

---

## ⚙️ Fonctionnalités

- Formulaire d’inscription avec vérification que les deux mots de passe entrés sont identiques.  
- Vérification que tous les champs sont remplis.  
- Stockage des comptes dans une base SQLite (`database.db`).  
- Formulaire de connexion.  
- Affichage du “prénom” (nom d’utilisateur) et du mot de passe après la connexion.  
- Message de succès ou d’erreur selon les cas.

---

## 🧱 Structure du projet

